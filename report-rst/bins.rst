The results of PORPID tagging for each sample. Only likely_real templates are
passed on for consensus generation. Other criteria for bin rejection are plotted
(indicated by color), with UMI length as a function of family size. 
